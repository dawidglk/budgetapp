import React from 'react';
import styled from 'styled-components';

const MainWrapper = styled.main`
    background: gray;
    flex-grow: 2;
`;

const Main = () => {
    return (
        <MainWrapper>
            Main
        </MainWrapper>
    )
};


export default Main